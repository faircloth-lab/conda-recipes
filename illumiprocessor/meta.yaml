package:
  name: illumiprocessor
  version: 2.0.5

source:
  fn: v2.0.5.tar.gz
  url: https://github.com/faircloth-lab/illumiprocessor/archive/v2.0.5.tar.gz
  sha1: 6add15031cb8f30846c8e9b6573a633725df32f8

build:
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bx-python = bx-python:main
    #
    # Would create an entry point called bx-python that calls bx-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 0

requirements:
  build:
    - python

  run:
    - python
    - trimmomatic

test:
    commands:
      - illumiprocessor --help

about:
  home: https://github.com/faircloth-lab/illumiprocessor
  license: BSD
